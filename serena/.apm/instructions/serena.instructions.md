---
description: Use always when reading or modifying code or files
---

IMPORTANT: When accessing, analyzing reading or writeing code or files always try to use tools that start with the name `serena_` instead of using other tools like `bash`, `glob`, `grep` or  `read`.

Best Practices
- Always read files first with serena_read_file before editing
- Use symbolic tools for structural changes
- Use regex tools for precise text manipulation
- Check references before refactoring